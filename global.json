{
  "// Comment": "Pins the .NET SDK so `dotnet fsi` script runs are reproducible (e.g. scripts/render-pixograms.fsx). rollForward=latestFeature accepts any feature-or-newer patch of 10.0.x.",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
